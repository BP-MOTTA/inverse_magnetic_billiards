# Inverse magnetic billiards
Considering the extension of the magnetic billiards. Here the billiards frontier is permeable so the charged particle can gro across it. We are considering two cases. First where the applied magnetic fields are paralell between them and whhen they are antiparalell
Enter to antiparalelo directory and look for antiparallel.py file, edit the Bin and Bout variables, change inital position and velocity then run

```
python3 antiparallel.py
``` 

you will see the trajectory plot and position vs angle data is saved as sdata.txt , plot this for the phase space.
It is kind of different for parallel directory. Enter to the parallel directory and look for the parallel.py file and run
```
python3 parallel.py
```
The code returns data and the plots.
